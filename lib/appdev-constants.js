/** Client-safe constants shared by appdev auth and board UI. */
export const PROTECTED_ASSIGNEE = 'FCS-建宏';

export const HARDCODED_MASTER_NAMES = [PROTECTED_ASSIGNEE];

/** Bump when shipping a new in-app changelog modal (localStorage dismiss key). */
export const RELEASE_NOTICE_ID = '2026-08';
